---
interface Props {
  class?: string;
  as?: 'div' | 'section' | 'header' | 'footer' | 'article' | 'main';
}

const { class: className = '', as: Tag = 'div' } = Astro.props;
---

<Tag class:list={['mx-auto w-full max-w-[1200px] px-5 md:px-10', className]}>
  <slot />
</Tag>
