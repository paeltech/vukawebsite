---
import { cn } from '@/lib/cn';

interface Props {
  text: string;
  class?: string;
  inverted?: boolean;
}

const { text, class: className = '', inverted = false } = Astro.props;
---

<p
  class={cn(
    'font-light text-[11px] uppercase tracking-category',
    inverted ? 'text-sand' : 'text-baobab',
    className,
  )}
>
  {text}
</p>
